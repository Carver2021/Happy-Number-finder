void main(numberStripper) {
  // i know there are errors in this.. it is a work in progress as I learn it will be better... I hope..

  int i = 2345;
  print('i= $i');

  int ones = (i % 10);

  int tens = (((i % 100) - ones) ~/ 10);

  int hundreds = ((i % 1000 - (ones + tens)) ~/ 100);

  int thousands = ((i - (ones + tens + hundreds)) ~/ 1000);

  int x =
      (ones * ones + tens * tens + hundreds * hundreds + thousands * thousands);

  print('x= $x');

  if (x > 1) {
    int ones1 = (x % 10);

    int tens1 = (((x % 100) - ones1) ~/ 10);

    int y = (ones1 * ones1 + tens1 * tens1);

    print('y = $y');

    if (y > 1) {
      int ones2 = (y % 10);

      int tens2 = (((y % 100) - ones2) ~/ 10);

      int z = (ones2 * ones2 + tens2 * tens2);
      print(' z = $z');
      if (z > 1) {
        int ones3 = (z % 10);
        if (z > 1) {
          int ones4 = (z % 10);
          print(ones4);
          int tens4 = (((z % 100) - ones4) ~/ 10);

          print(tens4);
          int a = (ones4 * ones4 + tens4 * tens4);

          print(' a = $a');
          if (a > 1) {
            int ones5 = (a % 10);
            print(ones5);
            int tens5 = (((a % 100) - ones5) ~/ 10);

            print(tens5);
            int b = (ones5 * ones5 + tens5 * tens5);
            print(' b = $b');
            if (b > 1) {
              int ones6 = (b % 10);
              print(ones6);

              print(ones6 * ones6);

              int tens6 = (((b % 100) - ones6) ~/ 10);

              print(tens6);

              int c = (ones6 * ones6 + tens6 * tens6);
              print('c = $c');
              if (c > 1) {
                int ones7 = (c % 10);
                print(ones7);
                print(ones7 * ones7);
                int tens7 = (((c % 100) - ones7) ~/ 10);
                print(tens7);
                int d = (ones7 * ones7 + tens7 * tens7);
                print('d = $d');

                if (d > 1) {
                  int ones8 = (d % 10);
                  print(ones8);
                  int tens8 = (((d % 100) - ones8) ~/ 10);
                  print(tens8);
                  int e = (ones8 * ones8 + tens8 * tens8);
                  print('e = $e');
                  if (e > 1) {
                    int ones9 = (e % 10);
                    print(ones9);
                    int tens9 = (((e % 100) - ones9) ~/ 10);

                    print(tens9);

                    int f = (ones9 * ones9 + tens9 * tens9);

                    print('f = $f');

                    if (f > 1) {
                      int ones10 = (f % 10);
                      int tens10 = (((f % 100) - ones10) ~/ 10);

                      int hundreds10 = ((f % 1000 - (ones10 + tens10)) ~/ 100);
                      int thousands10 =
                          ((f - (ones10 + tens10 + hundreds10)) ~/ 1000);

                      int g = (ones10 * ones10 +
                          tens10 * tens10 +
                          hundreds10 * hundreds10 +
                          thousands10 * thousands10);

                      print('g= $g');

                      if (g <= 1) {
                        print("$g is a happy number.");
                      } else {
                        print("$g is not a happy number after 10 iterations.");
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
